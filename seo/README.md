# The SEO content pipeline

Four stages, two human approval gates. Claude does the research, the writing and
the publishing; a person decides what gets written and what goes live.

```
  ┌── 1. IDEAS ────────┐   /seo-ideas
  │  DataForSEO research│   → writes Idea rows + briefs to Notion
  └──────────┬──────────┘
             │
  ┌── 2. APPROVE ──────┐   ** HUMAN **
  │  Idea → Approved    │   in Notion. Rejected = never propose again.
  └──────────┬──────────┘
             │
  ┌── 3. WRITE ────────┐   /seo-write
  │  post → repo branch │   → draft: true, AND the full text is
  │  + text into Notion │     posted into the Notion card
  └──────────┬──────────┘     Stage = Content review
             │
  ┌── 4. APPROVE ──────┐   ** HUMAN **
  │  read it in Notion  │   the draft is in the card — you never
  └──────────┬──────────┘   need the repo to approve
             │
  ┌── 5. PUBLISH ──────┐   /seo-publish
  │  date + merge main  │   → site publishes itself on the date
  └─────────────────────┘

  /seo-report — measures it, monthly, and feeds stage 1.
```

**The board:** https://app.notion.com/p/7fab09d9d487475590ce483e8f2979a6

**Strategies:** https://app.notion.com/p/daecab2ed9274dd2a5b9f290d31c96f6
The experiments. Each row is a bet with a hypothesis, one metric, a baseline and a
target. `/seo-ideas` generates from Planned and Running rows and links each idea
back; `/seo-report` fills in results and verdicts. Promote from Backlog to
Planned to steer what gets written.

**Competitors:** https://app.notion.com/p/76324edf739143a28ac98722e15c9d67

**The writing guide:** https://app.notion.com/p/3e2fcf50bd628108b76ae731c3804ef4
A human-readable copy of `strategy.md`, `seo-voice` and `humanizer` in one page.
The repo is the source of truth. **When a writing rule changes here, update that
page in the same session**, or the team will be working from stale rules.

You only ever edit one field on it: **Stage**. Everything else is filled by the
pipeline.

## Running it

| | |
|---|---|
| Monday | `/seo-ideas` — 3 ideas for the week. Approve in Notion. |
| Monday–Tuesday | `/seo-write` — one session per post. |
| Wednesday | Read the drafts. Approve. `/seo-publish`. |
| Monthly | `/seo-report`. |

To run stage 1 unattended, `/schedule` it weekly. Stages 2 and 4 are
deliberately not automatable — they are the point.

## How scheduled publishing works

There is no publishing server. Two pieces do it:

- `isLive` in `src/lib/blog.ts` hides any post whose `published` date is in the
  future — from its route, the index, the sitemap and the feed.
- The daily cron in `.github/workflows/deploy.yml` (06:15 UTC) rebuilds.

So a post is **merged to `main` the day it is approved, dated the day it should
appear**, and the first build on or after that date publishes it. Nothing has to
run on the day.

One failure mode to know: **GitHub disables scheduled workflows after 60 days
with no commits to the repo**, silently, with one email to the repo owner. If
posts stop appearing, check that before checking anything else. `/seo-report`
sweeps for it.

## The files

| | |
|---|---|
| `strategy.md` | The editorial rules. Change strategy here; the skills follow. |
| `keyword-map.tsv` | Every keyword the site ranks for and which URL owns it. Regenerated by `/seo-report`. |
| `baseline-2026-09-21.md` | The measured starting line. Everything is a delta against it. |
| `performance-log.md` | Appended by each report run. |
| `check-copy.mjs` | Build gate: fails on any em dash, en dash or spaced hyphen in pipeline copy. |
| `check-links.mjs` | Build gate: fails if a live page links to a post that is not live. Runs in `npm run check`. |
| `.claude/skills/seo-*` | The four stages. |
| `.claude/skills/seo-voice` | The house voice, extracted from `src/data/landings/`. The editing pass. |
| `.claude/skills/humanizer` | Vendored from github.com/blader/humanizer (MIT). 25 AI tells. Don't edit it. |

## One rule the gate enforces

A new post needs inbound links from existing posts, or Google reaches it last.
But those existing posts are live the moment they merge, while a scheduled post
is not — so adding the links early puts a 404 on two live pages for a week.

Markdown cannot be date-gated (`isLive` runs at page render; the rehype plugin's
output is cached across builds), so neither layer can flip a link on the publish
date. `seo/check-links.mjs` makes it a build error instead: **inbound links and
the post they point at go live in the same build, or the build fails.**

## What the numbers said on day one

61 posts, 257 ranked keywords, ~147 visits/month, and **zero top-3 rankings**.
The bottleneck is not how much gets published — it is that almost nothing
published is competitive. `strategy.md` is the response to that, and it is why
the mix is 2 new + 1 refresh rather than 3 new.

The single most valuable thing on the board: `airtable consultants`, 210/mo,
$30 CPC, position 32 — answered by a blog post about hiring consultants while
the actual service page ranks for nothing.
